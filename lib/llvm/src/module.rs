/// A module that hold necessary llvm code and state in relation to a wasm instance.
#[derive(Debug, Clone)]
pub struct Module {
    // TODO
}

impl Module {
    pub fn new() -> Self {
        Self {
            // TODO
        }
    }
}
