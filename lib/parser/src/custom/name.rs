use crate::{
    ir::Operator,
    parser::{Parser, ParserResult},
    stack::StackValue,
    ValueType::{self, *},
};
use wasmo_utils::verbose;

// Extends Parser implementation
impl<'a> Parser<'a> {
    // TODO
}
